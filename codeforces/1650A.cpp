#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t; cin >> t;
    for(int i = 0, i < t; i++) {
        // s.length() is odd
        string s; cin >> s;
        string c; cin >> c;
        
    }
    return 0;
}