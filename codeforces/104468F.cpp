#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);

    int t, n, a, b; cin >> t;
    while(t--) {
        cin >> n;
        a = n + 1;
        b = 2;
        cout << a << " " << b << "\n";
    }    
    
    return 0;
}